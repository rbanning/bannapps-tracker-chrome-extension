{
  "name": "banningapps-chrome-extension",
  "version": "1.0.0",
  "description": "A sample extension built by Banning Applications",
  "main": "index.js",
  "scripts": {
    "clean:extension": "rimraf extension",
    "copy:extension": "copyfiles -f bannapps-ext-app/dist/* extension",
    "build": "cd bannapps-ext-app && npm run build",
    "postbuild": "npm run clean:extension && npm run copy:extension"
  },
  "author": "Rob Banning",
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "dotenv": "^16.0.0",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "@netlify/functions": "^0.11.1",
    "@types/node": "^14.18.12",
    "airtable": "^0.11.1",
    "typescript": "^4.5.5"
  }
}
